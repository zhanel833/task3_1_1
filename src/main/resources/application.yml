spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test_db
    username: testuser
    password: ea836458
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
